export namespace IT {
    export interface Team {
        name: string,
        rol: string,
        image: string
    }

    export interface Project {
        title: string,
        description: string,
        image: string
    }
}